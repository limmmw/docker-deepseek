# running deepseek AI on ubuntu server using docker container

- run: 
    ```bash
    docker-compose up -d
    ```
- access ```http://localhost:3000```

- stop:
    ```bash
    docker-compose down
    ```

    